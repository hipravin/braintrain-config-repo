spring:
  profiles:
    active: dev

minigames:
  testprop: server_commmon_prop

---
spring:
  profiles: dev

server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

minigames:
  testprop: server_devprofile_prop

---
spring:
  profiles: dev2

minigames:
  testprop: server_dev2profile_prop
